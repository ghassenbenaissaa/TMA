<!DOCTYPE html>
<!--[if lt IE 7]>      <html class="no-js lt-ie9 lt-ie8 lt-ie7"> <![endif]-->
<!--[if IE 7]>         <html class="no-js lt-ie9 lt-ie8"> <![endif]-->
<!--[if IE 8]>         <html class="no-js lt-ie9"> <![endif]-->
<!--[if gt IE 8]><!--> <html class="no-js"> <!--<![endif]-->
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>{% block title %}Sign Up{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Découvrez les meilleures aventures de voyage avec nous. Explorez des destinations uniques et préparez-vous pour des expéditions inoubliables.">
    <meta name="keywords" content="voyage, voyages, voyager, voyages de noël, voyages de vacances, voyages de famille, voyages de travail, voyages de vacances, voyages de noël, voyages de famille, voyages de travail">
    <meta name="author" content="Ghassen Ben Aissa">

    {% block stylesheets %}
        <!-- Place favicon.ico and apple-touch-icon.png in the root directory -->
        <link rel="shortcut icon" href="{{asset('images/favicon-32x32.png')}}">
        <link href="{{asset('images/favicon.ico')}}" rel="icon">
        <link href='https://fonts.googleapis.com/css?family=Roboto:400,100,300,600,400italic,700' rel='stylesheet' type='text/css'>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">

        <!-- Animate.css -->
        <link rel="stylesheet" href="{{asset('css/animate.css')}}">
        <!-- Flexslider -->
        <link rel="stylesheet" href="{{asset('css/flexslider.css')}}">
        <!-- Icomoon Icon Fonts-->
        <link rel="stylesheet" href="{{asset('css/icomoon.css')}}">
        <!-- Magnific Popup -->
        <link rel="stylesheet" href="{{asset('css/magnific-popup.css')}}">
        <!-- Bootstrap  -->
        <link rel="stylesheet" href="{{asset('css/bootstrap.css')}}">

        <link rel="stylesheet" href="{{asset('css/style.css')}}">

        <link rel="stylesheet" id="theme-switch" href="{{asset('css/style.css')}}">
        <!-- End demo purposes only -->
        <!-- Modernizr JS -->
        <script src="{{asset('js/modernizr-2.6.2.min.js')}}"></script>
        <!-- FOR IE9 below -->
        <!--[if lt IE 9]>
	<script src="{{asset('js/respond.min.js')}}"></script>
	<![endif]-->
    {% endblock %}
</head>

<body>

<!-- Loader -->
<div class="fh5co-loader"></div>

<div id="fh5co-page">
    <section id="fh5co-header">
        <div class="container">
            <nav role="navigation">
                <ul class="pull-left left-menu">
                    <li><a href="{{ path('app_home') }}#fh5co-about">About</a></li>
                    <li><a href="{{ path('app_home') }}#fh5co-features-2">Features</a></li>
                    <li><a href="{{ path('app_home') }}#fh5co-pricing">Pricing</a></li>
                </ul>
                <h1 id="fh5co-logo">
                    <a href="{{ path('app_home') }}">
                        <img src="{{ asset('images/logo.png') }}" style="width: 100px" alt="Logo"><span>.</span>
                    </a>
                </h1>
            </nav>
        </div>
    </section>
    <!-- #fh5co-header -->

    <section id="fh5co-hero" class="js-fullheight" style="background-image: url('{{ asset('images/hero_bg.jpeg') }}') ;" data-next="yes">
        <div class="fh5co-overlay"></div>
        <div class="fh5co-intro js-fullheight" style="padding-top: 10px">
            <section id="custom-connexion">
                <div class="custom-container2">
                    <p class="custom-heading">Theme and Style</p>
                    <div style="text-align: left; padding-top: 10px; padding-bottom: 10px">
                        <p  class="fh5co-lead animate-box" style="font-size: 20px; color: #3d3d3d; margin: 0;">Choose how to categorize your travel memories.</p>
                        <p style="font-size: 16px; color: #3d3d3d; margin: 0;">This selection helps you organize cherished moments and relive your journeys in a personalized way.</p>
                    </div>
                    {# Afficher les messages flash d'erreur #}
                    {% for flashMessage in app.flashes('error') %}
                        <div class="error-message">
                            <span class="emoji">&#9888;</span> <!-- Emoji de danger -->
                            <span class="error-text">{{ flashMessage }}</span>
                        </div>
                    {% endfor %}
                    {{ form_start(form, {'attr': {'class': 'custom-form'}}) }}

                        <div class="custom-form-group">
                            {{ form_widget(form.type, {'attr': {'class': 'custom-form-control', 'placeholder': 'Type'}}) }}
                        </div>
                    <p class="fh5co-lead animate-box text-left" style="font-size: 20px">Choose Your Theme.</p>
                    <div class="row justify-content-center" >
                        <div class="col-md-4 col-sm-6 col-xxs-12 animate-box text-center">
                            <a href="{{ asset('images/dark.png') }}" class="fh5co-project-item image-popup">
                                <img src="{{ asset('images/dark.png') }}" alt="Dark Theme" class="img-responsive" style="width: 150px; height: 110px; margin-bottom: 10px; display: block; margin-left: auto; margin-right: auto;">
                                <div class="fh5co-text">
                                    <h3 style="font-size: 16px; margin-top: 0;">Dark</h3>
                                </div>
                            </a>
                        </div>

                        <div class="col-md-4 col-sm-6 col-xxs-12 animate-box text-center">
                            <a href="{{ asset('images/blue.png') }}" class="fh5co-project-item image-popup">
                                <img src="{{ asset('images/blue.png') }}" alt="Marine Blue Theme" class="img-responsive" style="width: 150px; height: 110px; margin-bottom: 10px; display: block; margin-left: auto; margin-right: auto;">
                                <div class="fh5co-text">
                                    <h3 style="font-size: 16px; margin-top: 0;">Marine Blue</h3>
                                </div>
                            </a>
                        </div>

                        <div class="col-md-4 col-sm-6 col-xxs-12 animate-box text-center">
                            <a href="{{ asset('images/pestachio.png') }}" class="fh5co-project-item image-popup">
                                <img src="{{ asset('images/pestachio.png') }}" alt="Pistachio Green Theme" class="img-responsive" style="width: 150px; height: 110px; margin-bottom: 10px; display: block; margin-left: auto; margin-right: auto;">
                                <div class="fh5co-text">
                                    <h3 style="font-size: 16px; margin-top: 0;">Pistachio Green</h3>
                                </div>
                            </a>
                        </div>
                    </div>


                    <div class="custom-form-group">
                            {{ form_widget(form.theme, {'attr': {'class': 'custom-form-control', 'placeholder': 'Thème'}}) }}
                        </div>

                    <div class="custom-form-group">
                        <input type="submit" value="Submit" class="custom-btn">
                    </div>
                    {{ form_end(form) }}
                </div>
            </section>
        </div>
    </section>

    <style>
        .error-message {
            color: #FF4500; /* Couleur orange */
            margin-bottom: 10px;
        }

        .error-message .emoji {
            font-size: 20px;
            margin-right: 5px;
        }

        .custom-form-row {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
        }

        .custom-form-group {
            margin-bottom: 15px;
        }

        .custom-form-group.half-width {
            width: calc(50% - 10px); /* Ajuster selon l'espace souhaité */
        }
    </style>

    <!-- END #fh5co-hero -->
    {% block body %}

    {% endblock %}


    <footer id="fh5co-footer" class="text-center">
        <div class="container">
            <div class="row">
                <div class="col-md-12 col-sm-12 col-xs-12 animate-box">
                    <div class="fh5co-footer-widget">
                        <ul class="fh5co-social">
                            <li><a href="#"><i class="icon-twitter-square"></i></a></li>
                            <li><a href="#"><i class="icon-facebook-official"></i></a></li>
                            <li><a href="#"><i class="icon-google-plus-square"></i></a></li>
                            <li><a href="#"><i class="icon-instagram"></i></a></li>
                            <li><a href="#"><i class="icon-youtube-square"></i></a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
        <BR>
        <div class="fh5co-copyright animate-box" style="padding: 20px 0;">
            <div class="container">
                <div class="row">
                    <div class="col-md-12">
                        <p class="text-center custom-font" style="margin: 0;">
                            <small>All Rights Reserved &copy; 2024, <span class="chn-text"><img src="{{ asset('images/signature_ghassen.png') }}" style="width: 85px; height: auto;"></span>.</small>
                        </p>
                        <br>
                    </div>
                </div>
            </div>
        </div>

    </footer>
    <div id="flash-messages-container" style="display: none;">
        {% for label, messages in app.flashes %}
            <div class="flash-messages" data-label="{{ label }}">
                {% for message in messages %}
                    <div class="flash-message">{{ message }}</div>
                {% endfor %}
            </div>
        {% endfor %}
    </div>

    <!-- END #fh5co-footer -->
</div>
<!-- END #fh5co-page -->

{% block javascripts %}

    <!-- jQuery -->
    <script src="{{asset('js/jquery.min.js')}}"></script>
    <!-- jQuery Easing -->
    <script src="{{asset('js/jquery.easing.1.3.js')}}"></script>
    <!-- Bootstrap -->
    <script src="{{asset('js/bootstrap.min.js')}}"></script>
    <!-- Waypoints -->
    <script src="{{asset('js/jquery.waypoints.min.js')}}"></script>
    <!-- Flexslider -->
    <script src="{{asset('js/jquery.flexslider-min.js')}}"></script>
    <!-- Magnific Popup -->
    <script src="{{asset('js/jquery.magnific-popup.min.js')}}"></script>
    <script src="{{asset('js/magnific-popup-options.js')}}"></script>

    <!-- For demo purposes only styleswitcher ( You may delete this anytime ) -->
    <script src="{{asset('js/jquery.style.switcher.js')}}"></script>
    <script src="{{asset('js/main.js')}}"></script>

    <script src="{{asset('js/jquery.cookie.js')}}"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.js"></script>

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const flashMessages = document.querySelectorAll('.flash-messages');

            flashMessages.forEach(function (flashMessageContainer) {
                const label = flashMessageContainer.getAttribute('data-label');
                const messages = flashMessageContainer.querySelectorAll('.flash-message');

                messages.forEach(function (messageElement) {
                    const message = messageElement.textContent;

                    // Configure SweetAlert based on the type of message
                    let swalOptions = {
                        title: message,
                        showConfirmButton: false,
                        timer: 2500 // Adjust the time as needed
                    };

                    if (label === 'success') {
                        swalOptions.icon = 'success';
                    } else if (label === 'error') {
                        swalOptions.icon = 'error';
                        swalOptions.title = 'Error';
                        swalOptions.html = message;
                        swalOptions.showConfirmButton = true;
                        swalOptions.timer = null;
                    } else if (label === 'warning') {
                        swalOptions.icon = 'warning';
                    } else if (label === 'info') {
                        swalOptions.icon = 'info';
                        swalOptions.title = 'Information';
                        swalOptions.html = message;
                        swalOptions.showConfirmButton = true;
                        swalOptions.timer = null;
                    }

                    // Display SweetAlert
                    Swal.fire(swalOptions);
                });
            });
        });

    </script>
    <style>
        div:where(.swal2-container) div:where(.swal2-popup) {
            display: none;
            position: relative;
            box-sizing: border-box;
            grid-template-columns: minmax(0, 100%);
            width: 32em;
            max-width: 100%;
            padding: 0 0 1.25em;
            border: none;
            border-radius: 5px;
            background: #fff;
            color: #545454;
            font-family: inherit;
            font-size:1.5rem;
        }
    </style>
    <script>
        $(function(){
            $('#colour-variations ul').styleSwitcher({
                defaultThemeId: 'theme-switch',
                hasPreview: false,
                cookie: {
                    expires: 30,
                    isManagingLoad: true
                }
            });
            $('.option-toggle').click(function() {
                $('#colour-variations').toggleClass('sleep');
            });
        });
    </script>
    <!-- End demo purposes only -->


{% endblock %}
</body>
</html>

